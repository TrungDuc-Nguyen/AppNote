<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.note">

    <application
        android:name=".database.myApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PassWord.Register"></activity>
        <activity android:name=".PassWord.Login" />
        <activity android:name=".EditNote.EditNote" />
        <activity android:name=".watchNote.watchNoteOnList" />
        <activity android:name=".addNote.NewNote" />
        <activity android:name=".Home.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>